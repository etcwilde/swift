string(TOLOWER "${SwiftStdlib_PLATFORM_OVERLAY}" SwiftOverlay_PLATFORM_OVERLAY)
list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/cmake/")
include("${SwiftOverlay_PLATFORM_OVERLAY}" REQUIRED)
